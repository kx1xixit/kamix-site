<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xmlns:media="http://search.yahoo.com/mrss/">
    <title>Kamix Blog</title>
    <link href="https://kamix.sdisk.us/feed.xml" rel="self" />
    <link href="https://kamix.sdisk.us" />
    <updated>2026-03-01T10:28:37-06:00</updated>
    <author>
        <name>Kane</name>
    </author>
    <id>https://kamix.sdisk.us</id>

    <entry>
        <title>Version 1.1.2-early-access</title>
        <author>
            <name>Kane</name>
        </author>
        <link href="https://kamix.sdisk.us/version-112-early-access/"/>
        <id>https://kamix.sdisk.us/version-112-early-access/</id>
            <category term="changelog"/>

        <updated>2026-03-01T10:24:16-06:00</updated>
            <summary type="html">
                <![CDATA[
                    <p>This update introduces a dedicated <code>checks</code> service and a pre-boot environment for VFS clearing, which now triggers a system reboot. Additionally, it streamlines the system by removing demo sprites and phasing out <code>vfs wipe</code> and configuration commands in favor of standard VFS operations.</p>
                ]]>
            </summary>
        <content type="html">
            <![CDATA[
                <p>This update introduces a dedicated <code>checks</code> service and a pre-boot environment for VFS clearing, which now triggers a system reboot. Additionally, it streamlines the system by removing demo sprites and phasing out <code>vfs wipe</code> and configuration commands in favor of standard VFS operations.</p>

<h3 id="adds">Adds:</h3>
<ul>
<li><code>checks</code>, the service &amp; VFS checking service.</li>
<li>A new pre-boot environment specifically for VFS clearing.</li>
</ul>
<h3 id="changes">Changes:</h3>
<ul>
<li>Removed the built-in “What can Kamix do?” demo sprites if they existed.</li>
<li><code>vfs clear</code> now reboots Kamix to a pre-boot environment.</li>
</ul>
<h3 id="removes">Removes:</h3>
<ul>
<li><code>vfs wipe</code> as it is now unsafe.</li>
<li><code>kx set-config</code> as it was superseded by <code>vfs write</code>.</li>
<li><code>kx list-config</code> as it was superseded by <code>vfs list</code>.</li>
</ul>

            ]]>
        </content>
    </entry>
    <entry>
        <title>Version 1.1.1-early-access</title>
        <author>
            <name>Kane</name>
        </author>
        <link href="https://kamix.sdisk.us/version-111-early-access/"/>
        <id>https://kamix.sdisk.us/version-111-early-access/</id>
            <category term="changelog"/>

        <updated>2026-03-01T10:23:42-06:00</updated>
            <summary type="html">
                <![CDATA[
                    <p>This update focuses exclusively on a stability fix that prevents the entire system from hanging during certain <code>vfss</code> actions.</p>
                ]]>
            </summary>
        <content type="html">
            <![CDATA[
                <p>This update focuses exclusively on a stability fix that prevents the entire system from hanging during certain <code>vfss</code> actions.</p>

<h3 id="adds">Adds:</h3>
<p><em>(Nothing.)</em></p><h3 id="changes">Changes:</h3>
<ul>
<li>Fixed the bug where some <code>vfss</code> actions would hang the entire system.</li>
</ul>
<h3 id="removes">Removes:</h3>
<p><em>(Nothing.)</em></p>
            ]]>
        </content>
    </entry>
    <entry>
        <title>Version 1.1.0-early-access</title>
        <author>
            <name>Kane</name>
        </author>
        <link href="https://kamix.sdisk.us/version-110-early-access/"/>
        <id>https://kamix.sdisk.us/version-110-early-access/</id>
            <category term="changelog"/>

        <updated>2026-03-01T10:23:18-06:00</updated>
            <summary type="html">
                <![CDATA[
                    <p>This update introduces the <code>shellms</code> manipulation service, the <code>vfs rm</code> command, and a <code>/RAM/busy</code> file to manage command concurrency. It also renames <code>kamix</code> to <code>bootl</code>, separates its commands into the new <code>kamixcs</code> service, and fixes a directory traversal bug in <code>vfs list</code>.</p>
                ]]>
            </summary>
        <content type="html">
            <![CDATA[
                <p>This update introduces the <code>shellms</code> manipulation service, the <code>vfs rm</code> command, and a <code>/RAM/busy</code> file to manage command concurrency. It also renames <code>kamix</code> to <code>bootl</code>, separates its commands into the new <code>kamixcs</code> service, and fixes a directory traversal bug in <code>vfs list</code>.</p>

<h3 id="adds">Adds:</h3>
<ul>
<li><code>shellms</code>,  the shell manipulation service.</li>
<li>New file in RAM (<code>/RAM/busy</code>) that enables or disables new commands from running if a command is already in progress.</li>
<li>Finalized <code>vfss</code> command: <code>vfs rm [path]</code>.</li>
</ul>
<h3 id="changes">Changes:</h3>
<ul>
<li>Renamed <code>kamix</code> to <code>bootl</code>.</li>
<li>Split Kamix commands into a separate service from <code>bootl</code>, named <code>kamixcs</code>.</li>
<li>Fixed the bug where <code>vfs list [path]</code> would misinterpret files as directories if your working directory is not <code>/</code> (root).</li>
</ul>
<h3 id="removes">Removes:</h3>
<p><em>(Nothing.)</em></p>
            ]]>
        </content>
    </entry>
    <entry>
        <title>Version 1.0.0-early-access</title>
        <author>
            <name>Kane</name>
        </author>
        <link href="https://kamix.sdisk.us/version-100-early-access/"/>
        <id>https://kamix.sdisk.us/version-100-early-access/</id>
            <category term="changelog"/>

        <updated>2026-03-01T10:18:49-06:00</updated>
            <summary type="html">
                <![CDATA[
                    <p>Kamix, version 1.0.0 is officially out! However, it’s an early-access build, so not everything is here and bugs may occur.</p><p>Since there are no changes to log (this is Kamix’s first post, after all), the usual “Added”, “Removed”, and “Changed” headers are not there.</p>
                ]]>
            </summary>
        <content type="html">
            <![CDATA[
                <p>Kamix, version 1.0.0 is officially out! However, it’s an early-access build, so not everything is here and bugs may occur.</p><p>Since there are no changes to log (this is Kamix’s first post, after all), the usual “Added”, “Removed”, and “Changed” headers are not there.</p>

<p>If you want to download Kamix’s source code (.pmp) or try Kamix <a href="https://use.kamix.sdisk.us">the intended way</a>, click on their respective links.</p>
            ]]>
        </content>
    </entry>
</feed>
